

const initialState = {
    center: [-119.57305556, 37.73944444],
    zoom: [ 9 ],
}

export default function(state = initialState, action) {
    switch(action.type) {
        default:
            return state
    }
}

